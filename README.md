## C++ OpenGL and networking project that simulates smoke in a 3d voxel world

### There are 4 builds for this project: 

1: Client only build which runs independently

2: Server build which runs the backend for networking clients.

3: Client networking build that runs with the server.

4: Web built version of the client that runs with the server.

