## C++ OpenGL and networking project that simulates smoke in a 3d voxel world

### Builds for this project: 

- Client only build which runs independently

- Server build which runs the backend for networking clients.

- Client networking build that runs with the server.

